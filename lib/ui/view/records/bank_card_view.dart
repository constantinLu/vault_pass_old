import 'package:flutter/material.dart';

class BankCardView extends StatelessWidget {
  const BankCardView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
